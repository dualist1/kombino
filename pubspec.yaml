name: kombino
description: A Flutter app for creating and managing outfit combinations.
version: 1.0.0
environment:
  sdk: '>=2.18.0 <3.0.0'
dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.4.0
  firebase_auth: ^4.2.0
  cloud_firestore: ^4.3.0
  firebase_storage: ^11.0.0
  image_picker: ^0.8.6
  provider: ^6.0.0
dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true